# 体調の隠れ相関発見ノート — 詳細設計仕様書

> **コンセプト**: ユーザーに分析させない。1日10秒のゆるい記録から、不調と生活要因の「隠れた関連」を自動で見つけ、統計用語を使わない生活言語の仮説として、控えめに提示する個人用ツール。

- **対象規模**: 個人で使う小さなツール（シングルユーザー）
- **主操作環境**: スマートフォン
- **実装言語**: Python
- **データ保持**: 原則ローカル（外部送信なし、健康データのため特に厳守）
- **公開想定**: GitHub（OSS）
- **バージョン**: 0.1（設計確定版）
- **最終更新**: 2026-05-19

---

## 目次

1. [背景と独自性](#1-背景と独自性)
2. [解決する課題](#2-解決する課題)
3. [安全配慮（最重要）](#3-安全配慮最重要)
4. [スコープ](#4-スコープ)
5. [想定ユーザーと利用シーン](#5-想定ユーザーと利用シーン)
6. [用語定義](#6-用語定義)
7. [機能要件](#7-機能要件)
8. [相関発見アルゴリズム](#8-相関発見アルゴリズム)
9. [仮説の言語化ルール](#9-仮説の言語化ルール)
10. [データモデル](#10-データモデル)
11. [アーキテクチャ](#11-アーキテクチャ)
12. [API設計](#12-api設計)
13. [画面・操作フロー](#13-画面操作フロー)
14. [通知設計](#14-通知設計)
15. [非機能要件](#15-非機能要件)
16. [MVPスコープと段階的拡張](#16-mvpスコープと段階的拡張)
17. [ディレクトリ構成](#17-ディレクトリ構成)
18. [テスト方針](#18-テスト方針)
19. [OSS公開にあたっての注意](#19-oss公開にあたっての注意)
20. [将来拡張](#20-将来拡張)

---

## 1. 背景と独自性

### 1.1 既存ツールの調査結果

体調記録系のアプリは多数存在する（頭痛ろぐ、頭痛ーる、harema 等）。これらを調査した結果、共通する特徴は以下のとおり。

- 多くが **気圧・天候という単一要因** と症状の関係に特化している
- 「AI分析」を謳うものも、分析対象は気圧などあらかじめ決まった軸が中心
- ユーザーが任意に定義した**複数の生活要因を横断**し、**前日ラグ**まで考慮して関連を自動抽出するものは見当たらない

OSS側では Quantified Self 系プロジェクト（QS Ledger 等）が「個人データ集約と相関可視化」の思想を持つが、各種サービスからデータを取得しノートブックで可視化するデータ分析者向けの構成であり、非専門ユーザーが完結して使える体験ではない。

### 1.2 本ツールの独自ポイント

| 観点 | 一般的な体調記録アプリ | 本ツール |
|---|---|---|
| 分析軸 | 気圧・天候など固定 | **ユーザー定義の任意要因を横断** |
| 時間関係 | 当日のみ | **前日ラグまで考慮** |
| 出力 | グラフ・レポート | **生活言語の仮説文 + 検証提案** |
| 関与度 | ユーザーが解釈 | **解釈・分析はツールが肩代わり** |
| データ | クラウド前提が多い | **ローカル完結を既定** |

「任意要因の横断 × ラグ考慮 × 生活言語化 × 小実験で検証 × ローカル完結」という組み合わせに新規性があり、既存OSS・製品の焼き直しではない。

---

## 2. 解決する課題

「なんとなく不調」の多くは、睡眠・食事・天候・運動・ストレスなど複数要因の組み合わせで起きるが、人は自分の生活と症状の関係を正確には記憶できない。記録アプリは数多いが、記録から関係性を見つけて言葉で返してくれるものは少なく、結果として大量の記録が「眺めるだけ」で死蔵される。

本ツールはその「分析と気づき」をツール側が肩代わりする。

---

## 3. 安全配慮（最重要）

> 本章は機能要件に優先する。設計・実装で迷ったら本章に従う。

健康・症状の記録は、扱い方を誤るとユーザーに害を与えうる。設計調査の過程で、専門家による次の指摘が確認された。

- **ノセボ効果のリスク**: 症状を綿密に記録・モニタリングすること自体が、ネガティブな予期を通じて症状を悪化させる場合がある。記録をやめたら発作のない日が戻った、という事例も報告されている。
- **過剰な行動制限のリスク**: 「何もかもが原因に思えて」極端な食事・行動制限に陥る危険がある。

これを踏まえ、本ツールは次を必須の設計原則とする。

| ID | 原則 | 具体策 |
|---|---|---|
| SAFE-1 | 記録は最小限・低頻度に保つ | 1日1回・10秒。項目数に上限（既定6、推奨3〜4） |
| SAFE-2 | 不安を煽らない | 症状を強調表示・カウントダウン・連続日数の煽りをしない |
| SAFE-3 | 因果と断定しない | すべての仮説に「関連であり原因とは限らない」を必ず併記 |
| SAFE-4 | 過剰制限を抑制 | 「除去」を強く勧めない。提案は小さな試行にとどめる |
| SAFE-5 | 医療の代替にしない | 受診の代替ではない旨を明示。重い症状時は専門家相談を促す |
| SAFE-6 | 撤退の自由 | いつでも記録停止・全削除が容易。それを推奨し得ることも明記 |
| SAFE-7 | 反芻を助長しない | 振り返りUIで否定的感情の長時間反芻に誘導しない |

この安全配慮自体が、単純な記録アプリにはない本ツールの設計上の差別化点でもある。

---

## 4. スコープ

### 4.1 本ツールがやること

- ユーザー定義の少数項目（症状・要因）の1日10秒記録
- 天候・気圧・曜日など自動取得可能な外部要因の自動付与
- 一定データ蓄積後、症状と要因（当日＋前日ラグ）の関連の定期走査
- 効果量が一定以上の関連のみを、生活言語の仮説として控えめに提示
- 仮説に対する小さな検証（軽い行動実験）の提案と前後比較

### 4.2 本ツールがやらないこと（明示的非スコープ）

- 診断・医療アドバイス（一切行わない）
- 多要因の機械学習モデルによる予測（少データで過学習し誤った気づきを生むため）
- 連続記録の強制やゲーミフィケーション（SAFE-2）
- 強い除去指導・食事制限指導（SAFE-4）
- クラウド共有・SNS的比較（v1非対象、健康データのため特に慎重）

---

## 5. 想定ユーザーと利用シーン

| ユーザー像 | 困りごと | 本ツールでどうなるか |
|---|---|---|
| 慢性的な頭痛持ち | 誘因が分からず対処が後手 | 「前日の睡眠が短い日に頭痛が多い傾向」を提示 |
| 気分の波がある人 | 何が引き金か自覚しにくい | 曜日・睡眠・天候との関連を控えめに示す |
| 胃腸が弱い人 | 特定の食事との関係が曖昧 | 食事タグと症状の共起を集計し示す |
| 体調管理をしたい一般層 | 記録しても活かせない | 記録が自動で気づきに変わる |

---

## 6. 用語定義

| 用語 | 定義 |
|---|---|
| 記録項目 (Metric) | 追跡対象。役割は「症状(目的)」か「要因(説明)」 |
| 日次記録 (Entry) | ある日のある項目の値。1日1セット |
| 自動要因 (AutoFactor) | 天候・気圧・曜日など自動付与される要因 |
| ラグ (lag) | 要因が症状に先行する日数。0=当日、1=前日 |
| 効果量 (effect size) | 関連の強さの指標。提示の足切りに用いる |
| 気づき (Insight) | 抽出され言語化された仮説 |
| 検証 (Experiment) | 仮説を確かめる小さな行動実験と前後比較 |

---

## 7. 機能要件

各要件にID（`FR-x`）を付す。安全配慮（SAFE-x）と矛盾する場合はSAFEを優先する。

### FR-1 記録項目の設定
- FR-1.1 ユーザーは症状（目的変数）と要因（説明変数）を定義できる。
- FR-1.2 各項目の型は「二値（あり/なし）」「連続（数値・スライダー）」「カテゴリ（タグ）」のいずれか。
- FR-1.3 項目数の上限を設ける（既定6）。超過追加時は安全配慮の説明を表示（SAFE-1）。

### FR-2 日次記録
- FR-2.1 1日1回、各項目をタップ／スライダーで入力。所要10秒を目標。
- FR-2.2 入力をスキップした日は欠損として扱い、補完しない。
- FR-2.3 過去日の後追い入力・修正が可能。
- FR-2.4 連続記録日数・達成バッジ等の煽りは表示しない（SAFE-2）。

### FR-3 自動要因付与
- FR-3.1 曜日は記録不要で自動付与。
- FR-3.2 天候・気圧は、ユーザーが地域を許可した場合のみ自動付与。位置情報は粗い粒度に限定し、ローカル保持（SAFE-5/プライバシー）。

### FR-4 相関の走査
- FR-4.1 有効記録日数が閾値（既定: 症状の発生・非発生がそれぞれ最低数件、目安として全体14日以上）に達するまで走査しない。
- FR-4.2 各（症状, 要因, ラグ∈{0,1,2}）の組について関連を評価する（[8章](#8-相関発見アルゴリズム)）。
- FR-4.3 効果量が足切り未満のものは提示しない（弱い関連で不安を煽らない: SAFE-2）。
- FR-4.4 多重比較を抑制するため、候補数を絞り、効果量と最低サンプル数の二重基準で選別する。

### FR-5 仮説の提示
- FR-5.1 統計用語を使わず生活言語で提示（[9章](#9-仮説の言語化ルール)）。
- FR-5.2 すべての仮説に「これは関連であって原因とは限りません」を必ず併記（SAFE-3）。
- FR-5.3 強さは『はっきり / ややあり / 弱い』の3段階表現に丸める。数値は既定で出さない。
- FR-5.4 一度に提示する仮説は最大1〜2件。羅列しない（SAFE-2/SAFE-7）。

### FR-6 検証ループ
- FR-6.1 仮説に対し「試してみる」を選ぶと、小さな行動実験を提案（例: 3日間だけ就寝を30分早める）。
- FR-6.2 提案は「足す/早める」など穏当なものを優先し、強い除去・制限は提案しない（SAFE-4）。
- FR-6.3 実験期間の前後で症状頻度を比較し、結果を控えめに提示（「変化は見られませんでした」も等価に扱う）。

### FR-7 セーフティ機能
- FR-7.1 オンボーディングで、本ツールが医療の代替でないこと、記録が負担なら中断してよいことを明示（SAFE-5/SAFE-6）。
- FR-7.2 設定からワンタップで「記録の一時停止」「全データ削除」が可能（SAFE-6）。
- FR-7.3 重い症状を示す入力が続く場合、専門家相談を穏やかに促す静的メッセージを表示（診断は一切しない）。

### FR-8 設定・データ管理
- FR-8.1 全データのエクスポート／インポート（JSON）。
- FR-8.2 全データ削除（即時・確認付き）。
- FR-8.3 走査の感度（足切りの強さ）を「控えめ／標準」から選択。既定は控えめ。

---

## 8. 相関発見アルゴリズム

### 8.1 設計原則

- 個人の少数項目・少データが前提。**重い機械学習は使わない**（過学習で誤った気づきを生むため）。
- 解釈可能性を最優先。理由を一言で説明できない手法は採らない。
- 「関連の有無」を断定せず、効果量の大小として控えめに扱う。
- 偽の発見を恐れる側に倒す（足切りは厳しめ＝控えめが既定）。

### 8.2 入力

日次に整列したテーブル。行=日付、列=各記録項目＋自動要因。欠損は欠損のまま。

### 8.3 関連の評価方法（型の組み合わせ別）

症状（目的）と要因（説明）の型に応じて、標準的で説明可能な指標のみ用いる。

| 症状の型 | 要因の型 | 指標 | 効果量 |
|---|---|---|---|
| 二値 | 連続 | 症状あり群 / なし群の平均差 | 標準化平均差（Cohen's d 相当） |
| 二値 | 二値/カテゴリ | 2×2の共起 | リフト値、リスク差 |
| 連続 | 連続 | 単調関係 | 順位相関（Spearman） |
| 連続 | 二値/カテゴリ | 群間の中央値差 | 標準化差 |

p値は内部的に補助参照する場合でも、**UIには出さない**（非専門ユーザーの誤解防止）。提示可否は効果量と最低サンプル数で決める。

### 8.4 ラグの扱い

要因の値を 0/1/2 日シフトし、それぞれについて 8.3 を評価する。最も効果量が大きいラグを代表として保持。ラグ＞0が選ばれた場合のみ「前日の」「一昨日の」と言語化する。

### 8.5 足切り基準（既定: 控えめ）

すべて満たした関連のみ Insight 化する。

```
- 有効サンプル数: 症状あり・なしが各 n_min 以上（既定 n_min = 5）
- 効果量: |d| >= 0.5（中程度以上）または リフト >= 1.5
- 全体記録日数: >= 14 日
- 同時に提示する Insight 数: <= 2（効果量上位のみ）
```

「標準」感度では `n_min=4`, `|d|>=0.4`, `リフト>=1.3` に緩和。それ以上の緩和設定は用意しない（誤発見抑制のため）。

### 8.6 強さ表現への写像

| 効果量 | 表現 |
|---|---|
| `|d| >= 0.8` または `lift >= 2.0` | はっきり |
| `0.5 <= |d| < 0.8` または `1.5 <= lift < 2.0` | ややあり |
| それ未満 | （提示しない） |

### 8.7 擬似コード

```python
from statistics import mean, pstdev, median

def cohens_d(group_a, group_b):
    """二群の標準化平均差。データ不足時は None。"""
    if len(group_a) < 2 or len(group_b) < 2:
        return None
    na, nb = len(group_a), len(group_b)
    va, vb = pstdev(group_a) ** 2, pstdev(group_b) ** 2
    pooled = (((na - 1) * va + (nb - 1) * vb) / (na + nb - 2)) ** 0.5
    if pooled == 0:
        return None
    return (mean(group_a) - mean(group_b)) / pooled

def scan(daily_rows, symptom, factor, lags=(0, 1, 2),
         n_min=5, d_min=0.5):
    """daily_rows: 日付順の dict のリスト。
    返り値: 最良ラグの関連、または None（足切り未満）。"""
    best = None
    for lag in lags:
        pairs = []
        for i in range(lag, len(daily_rows)):
            s = daily_rows[i].get(symptom)
            f = daily_rows[i - lag].get(factor)
            if s is None or f is None:
                continue
            pairs.append((s, f))

        # 症状二値 × 要因連続 のケース
        with_symptom    = [f for (s, f) in pairs if s == 1]
        without_symptom = [f for (s, f) in pairs if s == 0]
        if len(with_symptom) < n_min or len(without_symptom) < n_min:
            continue

        d = cohens_d(with_symptom, without_symptom)
        if d is None or abs(d) < d_min:
            continue

        if best is None or abs(d) > abs(best["effect"]):
            best = {"lag": lag, "effect": d,
                    "n_pos": len(with_symptom),
                    "n_neg": len(without_symptom)}
    return best  # None なら「気づきなし」= 沈黙する
```

「気づきなし」を異常ではなく**正常な結果**として扱うこと（SAFE-2）。

---

## 9. 仮説の言語化ルール

抽出された関連は、必ず次のテンプレートに沿って生活言語へ変換する。

### 9.1 テンプレート

```
{症状ラベル}が出た日は、{ラグ表現}{要因ラベル}が
普段より{方向}傾向があります（{強さ}）。
※これは関連であって、原因とは限りません。
```

- `{ラグ表現}`: lag=0→（空）／lag=1→「前日の」／lag=2→「一昨日の」
- `{方向}`: 平均差の符号から「短い／長い／多い／少ない」を選択
- `{強さ}`: 8.6 の3段階（「はっきり」「ややあり」。それ未満は生成しない）

### 9.2 生成例

- 「頭痛が出た日は、前日の睡眠時間が普段より短い傾向があります（ややあり）。※これは関連であって、原因とは限りません。」
- 「気分の落ち込みは、雨の日に多めの傾向があります（はっきり）。※これは関連であって、原因とは限りません。」

### 9.3 禁止事項（SAFE準拠）

- 「〜が原因です」「〜をやめましょう」など断定・指示をしない（SAFE-3/SAFE-4）。
- 数値（p値・相関係数・%）を既定で本文に出さない（SAFE-2）。
- 不安を強める語（「危険」「悪化し続ける」等）を使わない（SAFE-2）。
- 複数の仮説を一度に列挙しない（最大1〜2件: FR-5.4）。

---

## 10. データモデル

SQLite を既定とする。すべてローカル。健康データのため外部送信しない。

### 10.1 テーブル定義

#### `metrics`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PK | 項目ID |
| name | TEXT | NOT NULL | 表示名 |
| role | TEXT | NOT NULL | 'symptom' / 'factor' |
| dtype | TEXT | NOT NULL | 'binary' / 'continuous' / 'category' |
| created_at | TEXT | NOT NULL | 作成日時(ISO8601) |
| archived | INTEGER | DEFAULT 0 | 論理削除 |

#### `entries`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PK | 記録ID |
| date | TEXT | NOT NULL | 対象日(YYYY-MM-DD) |
| metric_id | INTEGER | FK→metrics.id | 対象項目 |
| value | REAL | NULL | 値（categoryは別表参照） |
| UNIQUE | | (date, metric_id) | 1日1項目1値 |

#### `auto_factors`
| カラム | 型 | 説明 |
|---|---|---|
| date | TEXT | 対象日 |
| weekday | INTEGER | 0=月〜6=日 |
| weather | TEXT | 取得できた場合のみ |
| pressure_hpa | REAL | 取得できた場合のみ |

#### `insights`（派生・再生成可能）
| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER | PK |
| symptom_id | INTEGER | FK→metrics.id |
| factor_ref | TEXT | metric_id または自動要因キー |
| lag | INTEGER | 0/1/2 |
| effect | REAL | 効果量（内部用、UI非表示） |
| strength | TEXT | 'clear' / 'mild' |
| text | TEXT | 言語化済み本文 |
| detected_at | TEXT | 検出日時 |
| dismissed | INTEGER | ユーザーが非表示にしたか |

#### `experiments`
| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER | PK |
| insight_id | INTEGER | FK→insights.id |
| start_date | TEXT | 開始日 |
| end_date | TEXT | 終了日 |
| intervention | TEXT | 介入内容（穏当なもの） |
| before_rate | REAL | 前期間の症状頻度 |
| after_rate | REAL | 後期間の症状頻度 |
| conclusion | TEXT | 控えめな結論文 |

#### `settings`（単一行）
| カラム | 型 | 既定 | 説明 |
|---|---|---|---|
| sensitivity | TEXT | 'conservative' | 'conservative' / 'standard' |
| notify_time | TEXT | '21:00' | 記録リマインド時刻 |
| paused | INTEGER | 0 | 記録一時停止フラグ |

> **設計上の重要点**: 一次データは `metrics` / `entries` / `auto_factors` / `experiments`。`insights` はいつでも全消去・再生成できる派生データ。バックアップ対象は一次データのみで十分。

### 10.2 ER概略

```
metrics 1 ──< entries
metrics 1 ──< insights (symptom側) ── 1< experiments
auto_factors (日付キーで entries と結合)
settings (singleton)
```

---

## 11. アーキテクチャ

### 11.1 方針

案B（消耗品ストック予測）と**記録・通知・ローカル保存の基盤を共通化できる**設計とする。スマホ操作・Python実装の制約から、ローカルWebアプリ方式を推奨する。

- バックエンド: Python（FastAPI もしくは Flask）。ローカル起動。
- フロントエンド: スマホブラウザ向け軽量HTML/JS。PWA化（ホーム画面追加・オフライン・ローカル通知）。
- DB: SQLite ファイル1個。
- スケジューラ: 1日1回、(1) 記録リマインド、(2) 条件を満たせば相関走査、のバッチ。

### 11.2 構成図

```
┌─────────────── スマホ ───────────────┐
│  PWA (HTML/JS)                        │
│   - 10秒記録UI                         │
│   - 気づき表示（最大1〜2件）           │
│   - 検証ループ / 設定（停止・削除）     │
│   - ローカル通知（記録リマインド）      │
└───────────────┬───────────────────────┘
                │ HTTP (localhost / LAN)
┌───────────────▼───────────────────────┐
│  Python バックエンド (FastAPI/Flask)   │
│   - REST API (12章)                    │
│   - 相関エンジン (8章) ※単一モジュール  │
│   - 言語化モジュール (9章)              │
│   - 日次バッチ                          │
└───────────────┬───────────────────────┘
                │
        ┌───────▼────────┐
        │ SQLite (1ファイル)│
        └────────────────┘
```

### 11.3 案Bとの基盤共通化

| 共通化できる層 | 内容 |
|---|---|
| 記録UI基盤 | ワンタップ／少入力の記録コンポーネント |
| 通知パイプライン | 1日1回バッチ＋ローカル通知 |
| ローカルストレージ | SQLite＋エクスポート/インポート |
| 差分（非共通） | 解析エンジン（B=時系列予測 / C=相関抽出） |

---

## 12. API設計

REST/JSON。ローカル前提のため認証はv1省略（[15章](#15-非機能要件)）。

| メソッド | パス | 説明 | 対応FR |
|---|---|---|---|
| GET | `/api/metrics` | 記録項目一覧 | FR-1 |
| POST | `/api/metrics` | 項目作成 | FR-1 |
| PATCH | `/api/metrics/{id}` | 項目編集／アーカイブ | FR-1 |
| GET | `/api/today` | 当日の記録フォーム定義 | FR-2 |
| POST | `/api/entries` | 日次記録の保存（複数項目一括） | FR-2 |
| PATCH | `/api/entries` | 過去日の修正 | FR-2.3 |
| GET | `/api/insights` | 提示中の気づき（最大1〜2件） | FR-5 |
| POST | `/api/insights/{id}/dismiss` | 気づきを非表示 | FR-5/SAFE-7 |
| POST | `/api/insights/{id}/experiment` | 検証実験を開始 | FR-6 |
| GET | `/api/experiments` | 実験の状況・結果 | FR-6 |
| GET | `/api/settings` | 設定取得 | FR-8 |
| PATCH | `/api/settings` | 設定更新（感度・停止等） | FR-8/SAFE-6 |
| POST | `/api/pause` | 記録の一時停止 | SAFE-6 |
| GET | `/api/export` | 全データJSON出力 | FR-8.1 |
| POST | `/api/import` | JSON取り込み | FR-8.1 |
| DELETE | `/api/data` | 全データ削除（確認付き） | FR-8.2/SAFE-6 |
| POST | `/api/recompute` | 相関の即時再走査 | FR-4 |

### 12.1 主要レスポンス例

`GET /api/insights`
```json
[
  {
    "id": 12,
    "text": "頭痛が出た日は、前日の睡眠時間が普段より短い傾向があります（ややあり）。",
    "disclaimer": "これは関連であって、原因とは限りません。",
    "strength": "mild",
    "can_experiment": true
  }
]
```

`effect`（効果量数値）は**APIレスポンスに含めない**（UI非表示の徹底: SAFE-2）。

---

## 13. 画面・操作フロー

### 13.1 画面一覧

| 画面 | 目的 | 主要素 |
|---|---|---|
| オンボーディング | 安全配慮の説明と項目設定 | 免責文、項目選択（3〜4推奨） |
| 今日の記録 | 10秒記録 | 各項目のタップ／スライダー、保存 |
| 気づき | 仮説の確認 | 最大1〜2件のカード＋必ず併記の注意文 |
| 検証 | 小実験の実施と結果 | 介入提案、前後比較の控えめ表示 |
| 設定 | 個人化・撤退 | 感度、一時停止、全削除、エクスポート |

### 13.2 主要フロー

**オンボーディング（安全配慮を最初に）**
```
起動 → 「これは医療の代替ではない／負担なら中断してよい」提示
     → 症状1〜2個・要因2〜4個を選択 → 完了
```

**日常（記録）**
```
今日の記録 → 各項目を軽く入力（10秒） → 保存（煽り表示なし）
```

**気づき → 検証**
```
気づきカード（1件）→「試してみる」→ 穏当な小実験を提案
   → 期間終了後、前後を控えめに比較表示（変化なしも等価に提示）
```

### 13.3 UI原則（SAFE準拠）

- 症状を赤字・大アイコン・連続失敗表示で煽らない（SAFE-2）。
- 「気づきなし」は穏やかに正常表示（「今は目立った関連は見つかっていません」）。
- 注意文（原因とは限らない）は気づきと**同じ視認性**で常に併置（SAFE-3）。
- 一時停止・全削除は設定の浅い階層に置き、見つけやすくする（SAFE-6）。

---

## 14. 通知設計

- **頻度**: 1日1回の記録リマインドのみ（任意・既定21:00）。
- **抑制**: 気づき検出時も通知は静かな1通。煽り文言を使わない（SAFE-2）。
- **一時停止中**: 通知も停止（SAFE-6）。
- **無通知日**: 記録済みの日は追加通知しない。
- **実装**: PWAのService Worker＋Notification API によるローカル通知。サーバプッシュ不要。

---

## 15. 非機能要件

| 区分 | 要件 |
|---|---|
| プライバシー | 健康データを扱うため、ローカル保持・外部送信なしを**絶対方針**。位置情報は粗い粒度のみ・任意 |
| セキュリティ | v1はローカル/LAN前提。LAN公開時は localhost バインドを既定にするオプションを用意 |
| 安全 | [3章](#3-安全配慮最重要)のSAFE-1〜7を全機能で遵守。違反する実装はマージしない |
| 性能 | 個人利用（項目〜数個、記録〜数百日）で走査は1秒未満 |
| 可用性 | オフラインで記録・閲覧可能（PWAキャッシュ） |
| 移植性 | Python 3.10+ / 主要OS。依存最小 |
| 保守性 | 相関エンジンと言語化を独立モジュール化し差し替え可能に |
| 国際化 | v1は日本語。文言は将来i18n可能なよう分離 |

---

## 16. MVPスコープと段階的拡張

| 機能 | MVP (v0.1) | v0.2 | v1.0 | 理由 |
|---|:---:|:---:|:---:|---|
| 安全配慮（SAFE-1〜7） | ● | | | 最優先・最初から必須 |
| 項目設定／10秒記録 | ● | | | 価値検証の最小核 |
| 曜日の自動付与 | ● | | | 外部依存なしで実装可 |
| 単純な群間差・共起＋ラグ | ● | | | 少データに頑健・説明可能 |
| 生活言語化（3段階） | ● | | | 非専門ユーザー向けの要 |
| 一時停止／全削除 | ● | | | 撤退の自由（SAFE-6） |
| エクスポート／インポート | ● | | | バックアップ最低限 |
| 検証ループ（小実験） | | ● | | 記録蓄積後に有効化 |
| 天候・気圧の自動付与 | | ● | | 外部取得・任意機能 |
| PWA通知の作り込み | | ● | | まず気づきの価値を検証 |
| 周期性・季節性の解析 | | | ● | 少データでは過剰 |
| 案Bとの基盤統合 | | | ● | 別仕様書で扱う |

**MVPの合格基準**: 「10秒記録を続けるだけで、自分でも気づかなかった関連が、不安を煽られずに穏やかな仮説として届く」体験が成立すること。

---

## 17. ディレクトリ構成

```
hidden-correlation-note/
├── README.md
├── LICENSE
├── pyproject.toml
├── app/
│   ├── __init__.py
│   ├── main.py            # FastAPI/Flask エントリ
│   ├── api.py             # ルーティング（12章）
│   ├── models.py          # SQLiteスキーマ（10章）
│   ├── correlate.py       # 相関エンジン（8章）※差し替え可能に分離
│   ├── verbalize.py       # 言語化（9章）※SAFE禁止語チェック内蔵
│   ├── safety.py          # SAFE-1〜7の共通ガード
│   ├── scheduler.py       # 日次バッチ
│   └── notify.py          # 通知判定
├── web/                   # PWA
│   ├── index.html
│   ├── app.js
│   ├── sw.js              # Service Worker（通知/オフライン）
│   └── manifest.json
├── tests/
│   ├── test_correlate.py  # 相関ロジック（最重要）
│   ├── test_verbalize.py  # 禁止語・注意文併記の検証
│   ├── test_safety.py     # SAFEガードの回帰テスト
│   └── fixtures/
└── docs/
    └── SPEC_PlanC.md      # 本書
```

---

## 18. テスト方針

相関エンジンと安全配慮が本ツールの心臓であり、ここを重点検証する。

- **単体テスト（最優先）**: `correlate.py`
  - データ不足時に走査が走らず「気づきなし」を返すこと（SAFE-2）
  - 明確な関連を仕込んだ合成データで、正しいラグ・方向が選ばれること
  - 無相関データでフォールスポジティブを出さないこと（足切りの検証）
  - 感度「控えめ／標準」で足切りが意図どおり変わること
- **言語化テスト**: `verbalize.py`
  - 全出力に注意文（原因とは限らない）が併記されること（SAFE-3）
  - 禁止語（「原因」「やめましょう」「危険」等）が出力されないこと（SAFE-2/4）
  - 同時提示が最大2件に制限されること（FR-5.4）
- **安全ガードテスト**: `safety.py`
  - 一時停止中は走査・通知が止まること（SAFE-6）
  - 全削除が一次・派生すべてを消すこと
- **手動シナリオ**: 数十日の擬似データで、気づきの穏当さと注意文の視認性を目視確認

---

## 19. OSS公開にあたっての注意

GitHub公開を前提とするため、盗作・ライセンス・安全面の指針を明記する。

1. **独自実装であることの担保**: 相関ロジック（8章）は公知の統計指標（標準化平均差、リフト値、順位相関）の組み合わせであり、特定の既存OSSのコードを流用しない。設計・コードはゼロから書く。
2. **先行事例への敬意**: READMEに「体調記録アプリやQuantified Self系OSSは多数存在するが、本ツールは『任意要因の横断・ラグ考慮・生活言語化・安全配慮の作り込み・ローカル完結』で差別化する」と記し、誇大な独自性主張をしない。
3. **第三者コード混入時の出典明記**: ライブラリ利用にとどめ、コード片を借用する場合は出典とライセンスを `NOTICE`／コメントに明記。
4. **ライセンス選定**: MIT または Apache-2.0 を推奨（特許条項重視なら Apache-2.0）。`LICENSE` を必ず同梱。
5. **依存ライブラリのライセンス確認**: 採用前に互換性を確認し `THIRD_PARTY_LICENSES` にまとめる。
6. **個人の健康データを含めない**: サンプル・スクショに実在の健康記録を含めない。`.gitignore` でローカルDB（`*.sqlite`）を除外。
7. **医療免責の明記（必須）**: README・アプリ内オンボーディングに「本ツールは医療機器でも診断ツールでもなく、医療の代替にはならない。気になる症状は専門家に相談すること」を明記（SAFE-5）。
8. **安全設計の明示**: READMEに本ツールがノセボ効果等のリスクに配慮して設計されている旨と、その具体策（SAFE-1〜7）を記載し、貢献者にも遵守を求める（CONTRIBUTING に明記）。

---

## 20. 将来拡張

- **検証ループの高度化**: 介入の前後比較を、より頑健な手法で（ただし説明可能性は維持）。
- **外部要因の拡充**: 天候・気圧に加え、任意のゆるい外部データ（任意・ローカル取得のみ）。
- **案Bとの統合**: 「モノの消耗（消耗品予測）」と記録・通知基盤を共通化し、『生活の消耗センサー』として一本化（別仕様書）。
- **エクスポートの活用**: 医療者と共有したいユーザー向けに、要約PDFの手動エクスポート（共有はユーザーの明示操作に限る）。

---

## 付録A: 設計上の不変原則（実装中に迷ったらここへ戻る）

1. 安全配慮（SAFE-1〜7）は機能要件に優先する。迷ったら控えめ側に倒す。
2. ユーザーに分析させない。記録は1日1回・10秒。
3. 仮説には必ず「原因とは限らない」を同じ視認性で併記する。
4. 「気づきなし」「変化なし」は正常な結果として穏やかに扱う。
5. 一次データは記録系のみ。気づきはいつでも再生成できる。
6. データはローカル。健康データを外部に出さないことを絶対方針とする。
